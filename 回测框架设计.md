# 回测框架文档
## 一、工作流
- 1. 在 `main.py` 中调用 `prepare_data.py`，获取合并后的数据
- 2. 使用模型训练和预测，生成预测值，保存在 `./data/pre.pkl` 中（可选）
- 3. 通过 `strategy.py` 产生仓位信息，保存在 `./data/position.pkl` 中（可以在 `pre.pkl` 的基础上生成，也可以直接根据市场行情生成）
- 4. 输入 `position.pkl`，通过 `gen_order.py` 产生的交易记录，保存在 `./data/order.pkl` 中，包含每天的交易方向，开仓、平仓动作，手续费
- 5. 通过 `perfomance4.py` 计算交易的指标，并生成回测结果网页

## 二、文件管理
### 目录结构
- 1. `./data` 存放运算过程中产生的预测数据、交易记录
- 2. `./factor_cal` 存放因子计算代码
- 3. `./backtest` 存放回测代码
- 4. `./model` 存放模型文件
- 5. `./strategy` 存放策略文件
- 6. `./common` 中存放计算时间和日期的代码 

### 文件格式说明
- 1. `./data/pre.csv` 存放模型预测的收益率与市场行情合并后的表格，预测的收益率命名为 `pre`
- 2. `./data/order.csv` 存放交易记录
- 3. `./data/position.csv` 存放仓位与市场行情合并后的表格，仓位那一列命名为 `pos`，

### 数据划分
- 1. `train_data` 存放从 2010 到 `year-1` 年的期货数据，用于训练模型。
- 2. `prediction_data` 存放当前 `year` 的数据，用于训练好的模型预测当前年份的值。
- 3. `pre_prediction_data` 用于保存模型预测以后的值。
- 4. 其中前两个包含了所有因子的值，最后一个只包含当前年份的基本信息，最后与预测值合并。

## 三、回测参数
- 使用 `config.py` 输入回测参数
```py
global train_list

DATA_DIR = 'C:/wzx/KCL/dataset/future_data' # 数据目录
k_line_type = '1d' # K线类型
pre_flag = 1 # 预测标签
model_name ='xgb' # 模型名称，为空则说明不需要模型，直接使用策略生成仓位
strategy_name = 'rand_pos' # 策略名称
model_param = {'max_depth': 6} # 模型参数
strategy_param = {'t': 0.001} # 策略参数

# 输入参与回测的因子，为空则所有因子全部参与
train_list=['FCT_Ar_1@5','FCT_Ac_Tr_1@5','FCT_Cmf_1@5','FCT_Br_1@5']
# 需要预测的指标
target_list = [f'FCT_CHG@{pre_flag}']
enter_count = 0
# 需要进行回测的年份，用该年份以前的数据进行训练，该年份进行预测
years_list=[2022, 2023]
# 手续费率
FEE_FEE=0.0002
# 启动资金
AMOUNT_4_OPEN = 1000000

instruments= ['A','AG','AL','AP','AU','BU','C','CF','CJ','CS',
            'CU','EB','EG','FG','FU','HC','I','IC','IF','IH',
            'J','JD','JM','L','LU','LH','M','MA','NI','OI','P',
            'PB','PF','PG','PK','PP','RB','RM','RU','SA','SF',
            'SM','SN','SP','SR','SC','SS','TA','T','TF','UR','V','Y','ZN'] #期货类型
```

## 四、模型接口说明
- 模型文件存放在 `./model` 中，命名为 `model_name.py`，并在 `config.py` 中通过 `model_name='model_name'` 导入
```py
class MODEL():
    def __init__(self, instruments, x_train, y_train, x_test, x_pre_test, param):
        '''输入的数据都是 pd.dataframe 类型，后续在模型中处理成模型需要的数据类型'''
        self.x_train = x_train # 训练集的特征
        self.y_train = y_train # 训练集的标签
        self.x_test = x_test # 测试集的特征
        self.x_pre_test = x_pre_test # 存放市场数据与模型预测结果合并后的表格
        self.instruments = instruments # 期货品种
        self.Depth = param['max_depth'] # 模型需要的参数
        pass

    def run():
        '''
        通过这个接口调用整个模型，并生成 pre.csv
        输出收益率的属性列为 'pre'，需要将模型预测的结果向后移一个时间单位
        '''
```

## 五、策略接口说明
- 模型文件存放在 `./strategy` 中，命名为 `strategy_name.py`，并在 `config.py` 中通过 `strategy_name='strategy_name'` 导入
```py
class strategy():
    def __init__(self, x_pre_test, param):
        '''
        如果有模型预测的结果，则使用预测的结果
        否则直接使用市场数据
        '''
        if os.path.exists('./data/pre.csv'):
            self.x_pre_test = pd.read_csv('./data/pre.csv')
        else:
            self.x_pre_test = x_pre_test
        
        self.t = param['t'] # 策略参数

    def run(self):
        self.result = []
        for instrument in instruments:
            df = self.x_pre_test[self.x_pre_test['instrument'] == instrument].copy()
            # 仓位只有-1,0,1三种，属性列命名为 'pos'
            df['pos'] = np.where(df['pre'] > self.t, 1, np.where(df['pre'] < -self.t, -1 ,0)) 
            self.result.append(df)
        
        self.result = pd.concat(self.result, axis=0, ignore_index=True) 
        self.result.to_csv('./data/position.csv',index=False)
```

## 六、待完成任务
- 1. 通过 `position.csv` 直接生成 `order.csv`，需要修改 `gen_order.py`